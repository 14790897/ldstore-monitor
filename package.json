{
  "name": "ldstore-monitor",
  "version": "2.0.0",
  "description": "LD士多积分商城商品监控 - Cloudflare Worker",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --test-scheduled",
    "deploy": "wrangler deploy",
    "generate-vapid": "node scripts/generate-vapid.js"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250124.0",
    "typescript": "^5.7.0",
    "wrangler": "^3.99.0"
  },
  "dependencies": {
    "web-push-browser": "^0.0.3"
  }
}
